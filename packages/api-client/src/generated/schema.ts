/**
 * Placeholder for the generated OpenAPI schema. Run
 *
 *     pnpm --filter @tasheen/api-client generate
 *
 * with the API server running on :3000 to overwrite this file with the
 * real `paths` typings derived from the live OpenAPI document.
 *
 * Until then we expose `paths` as a record so `createClient<paths>` still
 * compiles — call sites that need real type safety should import the
 * Zod-validated wrappers from `@tasheen/validation` instead.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type paths = Record<string, any>;
