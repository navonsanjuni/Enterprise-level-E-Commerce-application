// Higher-level shared components: ProductCard, OrderTimeline, TierBadge,
// EditorialLookCard. Add as authored. These are domain-aware but UI-only —
// they take props, not data; data fetching is the consumer app's job.
export {};
